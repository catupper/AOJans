#include<stdio.h>
#include<math.h>
int main(){
	int n,sums[5000],a,b,i;
	for(;~scanf("%d",&a);){
		for(i=0;i++<a;sums[i]=sums[i-1]+b){
			scanf("%d",&b);
		}
		n=-9e9;
		for(i=0;i<a;i++){
			for(b=i;b<=a;b++){
				n=fmax(n,sums[b+1]-sums[a]);
			}
		}
		printf("%d\n",n);
	}
	return 0;
}
