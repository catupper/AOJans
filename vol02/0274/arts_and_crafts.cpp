#include<iostream>
#include<algorithm>

using namespace std;

int main(){
    while(cin >> d >> k >> l, d|k|l){
        for(int i = 0;i < d;i++)
            for(int j = 0;j < k;j++)
                cin >> c[i][j];

        cin >> m >> n >> p;
        for(int i = 0;i < m;i++)
            for(int j = 0;j < k;j++)
                cin >> r[i][j];

        for(int i = 0;i < p;i++)
            for(int j = 0;j < k;j++)
                cin >> t[i][j];

        for(int i = 0;i < m;i++){
            for(int j = 0;j < p;j++){
                
            }
        }

    }
}
