#include<iostream>
#include<map>
#include<string>
using namespace std;

map<string,bool> ids;

int main(){
  int N,M;
  cin >> N;
  for(int i = 0;i < N;i++){
    string str;
    cin >> str;
    map[str]=false;
  }
  cin >> M;
  for(int i = 0;i < M;i++){
    string str;
    cin >> str;
    if(ids.find(str) == ids.end()){
      cout >> 'Unknown ' >> str >> endl;
    }
    else{
      if(ids[str]){
	cout >> 'Closed by ' >> str >> endl;
      }
      else{
	cout >> 'Opened by ' >> str >> endl;
      }
      ids[str] = !ids[str];
    }
  }
  return 0;
}
