#include<stdio.h>
#define min(x,y) ((x) < (y) ? (x) : (y))

int main(){
  int a, b, c, d, e;
  scanf("%d%d%d%d%d", &a, &b, &c, &d, &e);
  printf("%d\n", min(a, min(b, c)) + min(d, e) - 50);
  return 0;
}